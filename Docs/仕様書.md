# 3DOriginal ゲーム仕様書

## 1. 文書情報

- プロジェクト名：3DOriginal
- ジャンル：3D探索型ヴァンサバライク・アクションRPG
- 制作目的：将来のMMORPG制作に必要な、キャラクター、戦闘、スキル、装備、敵AI、永続データ、シーン管理の基礎を学ぶ
- 初期版目標：2026年9月までに、ゲームの基本ループを最初から最後まで遊べる骨組みを完成させる
- 対象環境：既存C++／DirectX基盤（BaseFramework）
- 本書の優先順位：本書の確定仕様は、企画段階の古い提案より優先する

## 2. ゲーム概要

プレイヤーは魔法使いを操作し、安全地帯で魔法・補助効果・魔導具を準備して、少数の敵がいる外へ出撃する。戦闘中の魔法は自動発動し、プレイヤーは移動と位置取りに集中する。敵を倒して魔導具を集め、外の先にある教会で吸血鬼ボスの討伐を目指す。安全な場所でリコールを成功させると、戦利品を安全地帯の倉庫へ保存できる。

ゲームの中心となる判断は次の2点とする。

- 危険を冒して探索を続けるか、リコールして戦利品を確保するか
- すぐにボスへ向かうか、通常敵を倒して装備を整えるか

## 3. 対応範囲

### 3.1 初期版に含める機能

- 安全地帯、敵がいる外、吸血鬼が待つ教会の3ステージ
- プレイヤー移動と見下ろし追従カメラ
- 自動発動する炎・氷・雷の魔法
- 魔法ごとに1つ設定できる補助効果
- 通常敵3種類
- 吸血鬼ボス1体
- 魔導具のドロップ、取得、装備、倉庫保存
- リコール
- 死亡、復活、未保管戦利品の消失
- 自動使用する回復フラスコ
- ボス討伐、報酬、クリア表示
- 必要最小限のUIとセーブ

### 3.2 初期版に含めない機能

- オンライン通信、協力プレイ
- 複数職業
- 巨大なパッシブスキルツリー
- ランダム生成マップ
- ランダムな装備性能
- 多部位装備、クラフト、取引
- クエスト、NPC会話
- 出撃中のレベルアップ3択
- 炎・氷・雷それぞれの2個目以降の魔法

## 4. 基本ゲームループ

1. 安全地帯で魔導具、3つの魔法、各魔法の補助効果、回復フラスコを確認する。
2. 安全地帯から外へ出撃する。
3. 自動魔法で少数の敵を倒し、外を探索する。
4. 敵が落とした魔導具を拾い、一時所持品へ入れる。
5. 探索を続ける、リコールする、または教会へ入って吸血鬼ボスへ挑戦する。
6. リコール成功時は一時所持品を倉庫へ保存する。
7. 死亡時は一時所持品を失い、安全地帯で復活する。
8. 吸血鬼ボス討伐時は固有魔導具を獲得し、クリア後に安全地帯へ帰還する。

通常フィールドには制限時間を設けない。

## 5. 操作仕様

| 操作 | キーボード | ゲームパッド | 動作 |
|---|---|---|---|
| 移動 | WASD | 左スティック | カメラの向きを基準にXZ平面を移動する |
| カメラ回転 | マウス左右移動 | 右スティック | プレイヤーを中心に水平方向へ回転する |
| リコール | 専用キー（実装時に確定） | 専用ボタン（実装時に確定） | 5秒詠唱を開始する |
| UI決定・戻る | 実装時に割り当て | 実装時に割り当て | 安全地帯の装備・倉庫画面を操作する |

- 通常攻撃と魔法はすべて自動発動する。
- 初期版では手動攻撃、手動回避、ジャンプを使用しない。
- リコールだけは誤発動防止のため専用入力とする。
- 斜め入力は正規化し、縦横より速くならないようにする。
- カメラが回転しても、Wは画面奥、Sは画面手前、AとDは画面左右へ移動する。

## 6. シーン仕様

画面ごとに「何を表示するか」「どの処理をその画面で行うか」が分かる形で記載する。画面をまたぐ演出は、どちらのシーンが担当するかも明記する。

### 6.1 タイトル画面（TitleScene）

ゲームを起動した時に最初に表示する画面。

- タイトルロゴを表示する
- ゲームスタートボタンを表示する
- Exitボタンを表示する
- セーブデータがある場合は、ゲームスタート時に読み込む
- ゲームスタートを押すと安全地帯へ移動する
- Exitを押すとゲームを終了する

タイトル画面から安全地帯へ移動する時の暗転やフェードは、タイトル画面側で開始し、シーン変更後に安全地帯側で解除する。

### 6.2 ゲーム画面(GameScene)

#### 6.2.1 安全地帯

敵が出現しない準備ステージ。装備を整えて外へ向かう場所にする。

- プレイヤーを配置する
- 魔導具の装備変更と倉庫を使用できるようにする
- 3つの魔法と、それぞれの補助効果を設定できるようにする
- 回復フラスコの状態を確認できるようにする
- 外へ移動する入口を配置する
- 地面、壁、目印、復活地点を配置する

この画面で行う処理

- プレイヤーの移動、カメラ追従、地形との当たり判定
- 魔導具の装備変更と倉庫管理
- 3つの魔法スロットと補助効果の設定
- フラスコの全チャージ回復
- リコール成功後の一時所持品保存
- 死亡後のHP回復と復活地点への配置
- 外へのステージ移動
- ボス討伐後の報酬保存とクリア結果の表示
- 永続データの保存

この画面では行わない処理

- 敵の出現
- 敵からのダメージ
- 自動魔法による戦闘

#### 6.2.2 敵がいる外

安全地帯と教会の間にある探索ステージ。敵の数は少なめにし、移動、探索、装備収集を中心にする。

- 安全地帯から外へ出た地点を開始地点にする
- コウモリ、グール、吸血鬼の眷属を少数配置する
- 開けた道だけでなく、墓地や脇道にも装備を探す場所を作る
- 外の奥に教会への入口を配置する
- 教会へはゲーム開始直後から向かえるようにする
- 敵を全滅させなくても教会へ入れる

このステージで行う処理

- プレイヤー移動、カメラ追従、カメラ回転、地形衝突
- 敵の生成、追跡、攻撃、被弾、死亡、再出現
- 3魔法の自動発動と補助効果適用
- 魔導具のドロップ、取得、一時所持
- フラスコの自動使用とチャージ補充
- リコールの開始、進行、中断、成功
- 教会入口への接触と教会へのステージ移動
- プレイヤー死亡と安全地帯への遷移
- HUDと死亡画面の更新

#### 6.2.3 吸血鬼が待つ教会

外の奥にあるボス戦専用ステージ。教会へ入ると吸血鬼との戦闘を開始する。

- 教会内部、入口、祭壇を配置する
- 教会へ入ると入口を閉じる
- ボス戦中は外へ戻れない
- ボス戦中はリコールを使用できない
- 吸血鬼ボスと召喚されるコウモリを配置する
- ボスのHP、攻撃、段階変化を管理する
- プレイヤー死亡時は安全地帯へ戻す
- ボス討伐時は報酬とクリア表示を出す
- クリア後は安全地帯へ帰還する

死亡表示とクリア表示が出るまでは各ステージ側で処理する。表示後のボタン操作や結果確認を別画面に分ける場合は、その部分だけ専用の結果画面へ移す。

## 7. プレイヤー仕様

- 職業：魔法使い1種類
- 移動：入力方向へ一定速度で移動
- 向き：移動方向または自動攻撃対象方向へ回転。最終方式は操作感を見て決定する
- 攻撃はプレイヤー本体ではなく、周囲に浮いている3本の魔法の杖から行う
- 杖はプレイヤーへ追従し、一定の高さと距離を保って浮遊する
- 杖の先端を魔法弾とエフェクトの発生位置にする
- 攻撃時は杖が自動的に対象方向を向く
- 杖の向きや発射位置が変わっても、プレイヤーの移動方向には影響を与えない
- 当たり判定：地面、壁、敵攻撃と判定する
- 被弾後：短時間の無敵時間を設け、多段ヒットによる即死を防ぐ
- HPが0以下になった時点で死亡状態へ遷移する
- 装備、魔法、補助効果から最終能力値を再計算する

管理する主な能力値：

- 最大HP、現在HP
- 移動速度
- 魔法威力倍率
- 発動間隔倍率
- 効果範囲倍率
- 被ダメージ倍率

具体的な数値はプレイテストで決めるため、コードへ直書きせずデータ化する。

## 8. カメラ仕様

- プレイヤーを斜め上から見る見下ろしカメラ
- プレイヤー位置を滑らかに追従する
- マウスの左右移動で、プレイヤーを中心に水平方向へ回転する
- マウスを動かしていない時は現在の向きを維持する
- 移動方向へカメラを自動回転させない
- 上下の角度、高さ、プレイヤーとの距離は固定する
- カメラ回転中も魔法の自動照準には影響を与えない
- 急な回転と移動を補間し、画面酔いを抑える
- プレイヤー周辺と敵の攻撃予告が確認できる画角にする
- 壁や大きな物がプレイヤーを隠す場合は、将来的に半透明化を検討する
- 初期版では上下回転とズームを実装しない

## 9. 自動魔法仕様

魔法は3スロットへ各1つ装備する。初期版では同一魔法の重複装備を不可とする。各魔法はクールダウン終了、対象存在などの発動条件を満たすと自動発動する。

### 9.0 浮遊する3本の魔法の杖

- プレイヤーの周囲に魔法の杖を3本浮かせる
- 3本の杖は3つの魔法スロットへ1本ずつ対応する
- 杖1は炎、杖2は氷、杖3は雷の魔法を担当する
- 各杖は別々の対象、発動条件、クールダウンを管理できる
- 3本の杖はプレイヤーの位置に追従する
- 待機中は互いに重ならない位置で、プレイヤーの周囲を緩やかに浮遊する
- 魔法発動時は担当する杖が対象方向を向き、杖の先端から魔法を発生させる
- 対象がいない杖は攻撃せず、担当する待機位置へ戻る
- 杖同士および敵との物理的な当たり判定は持たせない
- 杖が壁へめり込む場合は表示位置だけを補正し、プレイヤーの移動を妨げない

### 9.1 ファイアボール（炎）

- 最も近い有効な敵を対象とする
- 杖の先端から対象方向へ弾を発射する
- 命中時、対象とその周囲へダメージを与える
- 必要処理：対象検索、弾生成、移動、衝突、範囲ダメージ、寿命管理

### 9.2 アイスノヴァ（氷）

- プレイヤーを中心とした円形範囲へダメージを与える
- 命中した敵へ一定時間の移動速度低下を付与する
- 必要処理：範囲判定、ダメージ、減速状態の付与・更新・解除

### 9.3 チェインライトニング（雷）

- 範囲内の敵1体を最初の対象とする
- 周辺の未命中の敵へ規定回数まで連鎖する
- 同一発動中は原則として同じ敵へ重複連鎖しない
- 必要処理：初期対象検索、次対象検索、命中対象記録、連鎖回数管理、ダメージ

### 9.4 補助効果

各魔法へ補助効果を1つ設定できる。

- 連射：発動間隔を短縮する
- 拡大：効果範囲を拡大する
- 追加発動：威力を下げる代わりに発射数、発動回数、または連鎖数を増やす

補助効果は魔法生成後ではなく、発動前の最終性能計算へ反映する。

## 10. 魔導具・装備仕様

- 装備枠：魔導具1枠
- 初期版の武器：同時に浮遊する魔法の杖3本
- 魔導書と水晶：基本戦闘の完成後に追加する補助魔導具候補
- レア度：通常、希少、伝説
- 性能：初期版では固定値。ランダム性能生成は行わない
- 初期装備：失われない見習いの杖を用意する

種類ごとの役割：

- 杖：プレイヤーの周囲に3本浮遊し、それぞれが担当する自動魔法を発動する
- 魔導書：魔法の発動間隔が短い
- 水晶：魔法の効果範囲が広い

レア度ごとの役割：

- 通常：基礎能力のみ
- 希少：追加効果1つ
- 伝説：固有効果1つ

初期版の目標数は固定性能の6～9種類とする。例として、炎属性ダメージを増やす火炎の杖、氷魔法の減速を強化する凍結の水晶、雷魔法の連鎖数を増やす雷鳴の魔導書を含める。

### 10.1 所持状態

- 装備中：現在装備している魔導具。死亡しても保持する
- 倉庫：安全地帯へ保存済みの魔導具。死亡しても保持する
- 一時所持：現在の出撃で拾い、まだ保存していない魔導具。死亡するとすべて失う

### 10.2 ドロップと取得

- 敵死亡時にドロップテーブルから抽選する
- フィールド上に取得物を生成する
- プレイヤー接触時に一時所持へ追加する
- リコール成功またはボス討伐後の帰還時に倉庫へ移す

## 11. 回復フラスコ仕様

- 種類：生命のフラスコ1種類
- 最大チャージ：3
- HPが最大HPの30%以下になった時に自動使用する
- 使用時にHPを即時回復する
- 使用後に短いクールダウンを設ける
- 規定数の敵を倒すごとに1チャージ補充する
- 安全地帯へ戻ると全チャージ回復する
- 死亡してもフラスコ自体は失わない
- HPが最大の場合やチャージ0の場合は使用しない

回復量、クールダウン、補充に必要な撃破数は調整データとする。

## 12. 敵仕様

### 12.0 最初に作る敵

最低限の骨組みでは、敵を次の2種類だけ先に作る。

- 雑魚敵：コウモリ1種類
- ボス敵：吸血鬼1種類

最初にコウモリで、出現、追跡、攻撃、被ダメージ、死亡、ドロップまでの共通処理を完成させる。その共通処理を利用して吸血鬼ボスを作る。グールと吸血鬼の眷属は、基本戦闘とボス戦が動いた後に追加する。

### 12.1 コウモリ

- 役割：素早く接近し、プレイヤーを包囲する
- 特徴：高移動速度、低HP、接触攻撃

### 12.2 グール

- 初期骨組みの完成後に追加する
- 役割：遅く接近し、進路を塞ぐ
- 特徴：低移動速度、高HP、接触攻撃

### 12.3 吸血鬼の眷属

- 初期骨組みの完成後に追加する
- 役割：距離を保ちながら遠距離攻撃を行う
- 特徴：適正距離維持、弾生成、射撃予告、クールダウン

### 12.4 共通処理

- 生息区域またはスポーン地点から生成する
- プレイヤーを検出して追跡・攻撃する
- 安全地帯へ侵入しない
- HP、被ダメージ、状態異常、死亡を管理する
- 死亡時に撃破数加算とドロップ抽選を行う
- 一定時間後に生息地点へ再出現する
- プレイヤーから極端に離れた個体は停止または再配置し、負荷を抑える

## 13. 吸血鬼ボス仕様

### 13.1 挑戦ルール

- 安全地帯から外を通り、ゲーム開始直後から教会へ直行できる
- 鍵、中ボス討伐、出現待ちは不要
- 教会へ入ると入口を閉じる
- 戦闘中はリコール不可
- 勝利または死亡まで退出不可
- 初期装備でも挑戦できるが、勝利は難しいバランスとする

### 13.2 攻撃

- 血の弾：扇状に複数の弾を発射する
- コウモリ召喚：周囲へコウモリを生成して包囲する
- 突進：攻撃方向を予告した後、直線上へ高速移動する
- 吸血陣：範囲内のプレイヤーへ継続ダメージを与え、ボスHPを回復する
- 第2段階：HPが50%以下になると攻撃間隔を短縮し、召喚数を増加する

すべての強攻撃は、移動だけで回避できる予告時間と表示を持たせる。

### 13.3 勝利処理

- ボスHPが0になると戦闘を停止する
- 固有魔導具「吸血鬼の魔導書」を報酬として付与する
- クリア画面を表示する
- 入力後または一定時間後に安全地帯へ帰還する
- 報酬、一時所持品、ボス討伐状態を保存する
- クリア後も再挑戦できる

## 14. リコール仕様

- 敵がいる外で専用入力により開始する
- 教会のボス戦中は開始できない
- 発動地点で5秒間詠唱する
- 詠唱中に移動または被弾すると中断する
- 中断後は通常状態へ戻る
- 成功すると一時所持品を保存対象として安全地帯へ移動する
- ボス戦中は使用できない
- 詠唱中は進行ゲージとエフェクトを表示する
- シーン遷移前後の二重保存・二重報酬を防止する

## 15. 死亡・復活・損失仕様

死亡時：

- 戦闘、入力、リコールを停止する
- 死亡演出後、安全地帯へ遷移する
- 一時所持している未保管の魔導具をすべて失う
- フィールドの敵、ドロップ、進行状態をリセットする

保持するもの：

- 装備中の魔導具
- 倉庫へ保存済みの魔導具
- 3つの魔法と補助効果設定
- 基本能力とフラスコ
- ボス討伐などの永続進行

安全地帯で復活する際はHPとフラスコを全回復し、復活地点へ配置する。

## 16. UI仕様

### 16.1 タイトル画面

- タイトルロゴ
- ゲームスタートボタン
- Exitボタン

ボタンを押した時は効果音を再生し、連続入力による二重遷移を防ぐ。

### 16.2 安全地帯

- 装備中の魔導具
- 倉庫の魔導具一覧
- 魔法3スロット
- 各魔法の補助効果スロット
- フラスコ情報
- 戦闘フィールド入口の案内
- ボス討伐済み表示

### 16.3 敵がいる外

- プレイヤーHPバー
- 魔法3スロットと発動状況
- フラスコの残りチャージ
- リコール詠唱ゲージ
- 一時所持品一覧または取得通知
- ボス戦中のボスHPバー
- 死亡表示

### 16.4 吸血鬼が待つ教会

- プレイヤーHPバー
- 魔法3スロットと発動状況
- フラスコの残りチャージ
- ボスHPバー
- 死亡表示
- クリア表示

UIはゲーム状態から表示を更新し、UI自身が戦闘ルールを所有しない。

### 16.5 死亡・クリア表示

現在いる外または教会を背景にしたまま表示する。

死亡時

- 死亡したことを表示する
- 今回失う未保管装備を確認できるようにする
- 入力後、安全地帯へ戻る

ボス討伐時

- クリアしたことを表示する
- 獲得した固有魔導具を表示する
- 入力後、または一定時間後に安全地帯へ戻る

背景の停止、暗転、ぼかしなどの画面効果までは戦闘フィールド側で行い、その上に表示する文字やボタンはUI側で行う。

## 17. セーブデータ仕様

保存するデータ：

- 装備中の魔導具ID
- 倉庫内の魔導具ID一覧
- 魔法3スロットの設定
- 各魔法の補助効果設定
- ボス討伐状態
- 必要になった場合の設定情報

保存しない一時データ：

- 一時所持品
- 敵のHP、位置、再出現時間
- フィールド上のドロップ
- リコール詠唱途中の状態
- 戦闘中のフラスコチャージ

セーブは安全地帯到着時、装備変更時、倉庫変更時、ボス報酬確定時に行う。データが存在しない場合や読み込みに失敗した場合は、安全な初期値で開始する。

## 18. データ設計方針

調整値とコンテンツ定義を処理コードから分離する。

- PlayerData：HP、移動速度、無敵時間
- SpellData：属性、威力、発動間隔、範囲、対象方式
- SupportEffectData：補正方式と補正値
- EquipmentData：ID、名称、種類、レア度、能力補正、固有効果
- EnemyData：HP、速度、攻撃力、攻撃間隔、ドロップテーブル
- BossData：HP、段階条件、各攻撃の値
- FlaskData：回復量、チャージ数、クールダウン、補充条件
- DropTableData：対象ID、確率、個数

各データは一意なIDを持ち、セーブデータでは表示名ではなくIDを保存する。

## 19. 推奨クラス・責務

現在の `BaseScene`、`SceneManager`、`KdGameObject` を基盤として、少なくとも次の責務へ分離する。

- `GameScene`：3ステージの管理とステージ切り替え
- `SafeZoneStage`：安全地帯全体の生成と進行
- `OutsideStage`：敵がいる外の生成と進行
- `ChurchStage`：教会、ボス戦、クリア処理
- `Player`：入力、移動、HP、死亡
- `FollowCamera`：見下ろし追従
- `EnemyBase`：敵共通ステータスと状態遷移
- `BatEnemy`／`GhoulEnemy`／`VampireMinionEnemy`：敵固有AI
- `VampireBoss`：ボス段階と攻撃選択
- `SpellController`：3スロット、クールダウン、自動発動
- `FloatingWand`：杖1本の追従、待機位置、対象方向への回転、魔法の発生位置
- `FloatingWandController`：3本の配置、魔法スロットとの対応、重なり防止
- `SpellBase`と各魔法：魔法固有処理
- `StatusEffectManager`：減速などの効果管理
- `EquipmentManager`：装備、一時所持、倉庫
- `DropManager`：抽選とドロップ生成
- `FlaskController`：自動回復とチャージ
- `RecallController`：詠唱、中断、帰還
- `GameProgress`：ボス討伐などの永続進行
- `SaveDataManager`：保存と読み込み
- `GameHUD`：状態の表示

敵、魔法、ドロップなどの参照は可能な範囲で弱参照またはID管理とし、削除済みオブジェクトへのアクセスを避ける。

## 20. 状態遷移

### 20.1 プレイヤー

`Normal → Recalling → Normal`（移動・被弾で中断）  
`Normal → Dead → SafeZone`  
`Recalling → RecallSuccess → SafeZone`  
`Normal → BossBattle → Clear → SafeZone`

### 20.2 敵

`Spawn → Chase／KeepDistance → Attack → Chase／KeepDistance → Dead → RespawnWait → Spawn`

### 20.3 ボス

`Idle → BattlePhase1 → BattlePhase2 → Dead → Clear`

## 21. アセット仕様

### 21.1 2D画像

企画上の最大見積もりは39枚とする。色違いや共通枠をコード描画・再利用する場合は削減できる。

| 分類 | 最大数 | 使用目的 |
|---|---:|---|
| UI共通 | 8 | HPバー、ボスHPバー、スロット、パネル、ボタン |
| 魔法・補助効果 | 6 | 3魔法と3補助効果のアイコン |
| 装備・フラスコ | 10 | 最大9装備とフラスコのアイコン |
| 状態・操作 | 7 | リコール、詠唱、未保管／保存済み、死亡、クリア |
| 環境テクスチャ | 8 | 安全地帯、外、墓地、教会、岩、木など |
| 合計 | 39 | 最大見積もり |

### 21.2 3Dモデル

最低限の骨組みで先に用意するモデルは次の6種類とする。

- 安全地帯
- 敵がいる外
- 吸血鬼が待つ教会
- プレイヤー（魔法使い）
- 雑魚敵（コウモリ）
- 吸血鬼ボス

加えて、攻撃表示に使う魔法の杖モデルを用意する。同じモデルを3本へ再利用する場合は最低7モデルのままとする。杖ごとに別デザインを用意する場合は最低9モデルとなる。

最終的に用意する主なモデル：

- 魔法使い
- コウモリ
- グール
- 吸血鬼の眷属
- 吸血鬼ボス
- 杖
- 魔導書
- 水晶
- 回復フラスコ
- リコール用転送石または魔法陣

地面、壁、柵、墓石、祭壇などは基本形状や同一モデルの再利用を優先する。

### 21.3 エフェクト（最低10種類）

- ファイアボール弾、爆発
- アイスノヴァ、減速表示
- チェインライトニング
- 被ダメージ、回復
- 装備ドロップ光
- リコール詠唱、成功

加えて、ボスの血の弾、吸血陣、突進予告が必要。

### 21.4 音

- 安全地帯BGM、外のBGM、教会・ボスBGM
- 各魔法、敵被弾、敵死亡、プレイヤー被弾、回復、取得
- リコール開始・成功、ボス討伐

## 22. 実装優先順位

1. `OutsideStage`、仮マップ、プレイヤー移動、追従カメラ、壁衝突
2. 敵1体の追跡、接触ダメージ、HP、死亡
3. ファイアボール、自動対象検索、敵死亡
4. 複数敵のスポーン、残り2種類の敵と魔法
5. 安全地帯と外の往復、装備ドロップ、一時所持、倉庫
6. リコール、死亡時損失、セーブ
7. 補助効果、魔導具6～9種、回復フラスコ
8. 教会ステージ、吸血鬼ボス、報酬、クリア
9. UI、エフェクト、音、バランス、不具合修正

### 実装項目チェック

完了した項目は、以前の仕様書と同じように取り消し線へ変更して進捗を残す。

- プレイヤー移動
- 見下ろしカメラ
- 地形との当たり判定
- プレイヤーのHPと死亡
- 敵の追跡、攻撃、段階的な追加
- 炎・氷・雷の自動魔法
- 魔法の補助効果
- 魔導具のドロップ、装備、倉庫
- リコール
- 回復フラスコ
- 安全地帯
- 敵がいる外
- 吸血鬼が待つ教会
- UI
- 死亡・クリア表示
- タイトル画面
- 音
- 吸血鬼ボス
- セーブと読み込み

## 23. 初期版の完成条件

- タイトルから安全地帯へ開始できる
- 安全地帯、外、教会の3ステージを移動できる
- プレイヤーが移動でき、壁を通り抜けない
- 3種類の敵と戦闘できる
- 炎・氷・雷の自動魔法がそれぞれ機能する
- 魔法ごとに補助効果を1つ設定できる
- 魔導具を取得、装備、一時所持、倉庫保存できる
- リコールにより戦利品を持ち帰れる
- 死亡すると未保管戦利品を失い、安全地帯で復活する
- フラスコが条件を満たすと自動使用される
- 開始直後からボスへ直行できる
- 教会のボス戦中は退出・リコールできない
- 吸血鬼ボスを倒すと報酬とクリア表示を得られる
- ゲームを終了して再起動しても保存済み装備を読み込める
- デバッグビルドで致命的なクラッシュ、進行不能、二重報酬が発生しない

## 24. 未決定・調整項目

次の項目は実装前またはプレイテスト後に決定する。未決定であることは、機能そのものを削除する意味ではない。

- 正式タイトルと詳細な物語
- 各キャラクター、魔法、装備の具体的な数値
- リコール、決定、戻るのキー割り当て
- 同一魔法の重複装備を将来許可するか
- 敵の再出現時間と同時出現数
- フラスコの回復量、撃破数、クールダウン
- フィールドの正確な広さ
- ボス討伐後の自動帰還までの待ち時間
- UI、3Dモデル、エフェクトの最終デザイン

未決定値は一時的な仮値をデータへ設定し、ゲームの一周が成立した後に調整する。
